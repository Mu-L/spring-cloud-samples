server:
  port: 8761

spring:
  application:
    name: nacos-config-sample
  cloud:
    nacos:
      username: nacos
      password: 7fDJZBbiLzO2
      config:
        serverAddr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
  config:
    import:
      - nacos:cloud-agent.properties?refreshEnabled=true&group=DEFAULT_GROUP